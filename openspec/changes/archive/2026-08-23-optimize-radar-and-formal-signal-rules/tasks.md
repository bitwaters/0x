## 1. 规则版本与持久化

- [x] 1.1 为候选增加机会类型、激活决策high-water，为固定池资格增加不可刷新参考价/时间，并为outbox增加已应用payload哈希的向后兼容SQLite migration；不增加雷达状态表或可评估序号。
- [x] 1.2 更新配置摘要并生成新规则版本，定义内部 `$10K–$300K`、Bonding `$10K–$100K`、正式 `$20K–$300K`、Top5/Top20、30分钟新池、`$500`成交额和5秒复核，保证旧快照保持原版本。
- [x] 1.3 编写migration/repository测试，覆盖旧数据库升级、发现/激活high-water隔离、资格参考价重启不变、outbox待编辑恢复和旧数据可查询。

## 2. 内部候选与公开雷达

- [x] 2.1 将 GMGN Top100 内部候选范围改为 `$10K–$300K`，保持正式处理不依赖公开雷达。
- [x] 2.2 实现 Bonding 公开门槛：`$10K–$100K`、当前1m Top5、连续两轮双榜或连续三次1m上升并最终Top5。
- [x] 2.3 实现真实池公开门槛与一币一卡编辑：`$20K–$300K`、当前1m Top20、最大池有效、GMGN流动性至少`$10K`；复用原outbox/message id表达Bonding升级和直接真实池发现。
- [x] 2.4 添加表驱动测试，覆盖单次/连续双榜、后排名次上升、Top5边界、未公开候选继续正式流程、编辑失败重试和雷达卡全生命周期。

## 3. 新池、复苏与激活基线

- [x] 3.1 实现池龄`≤30分钟`新池通道以及池龄`>30分钟`、Top20持续热度确认的复苏通道，移除绝对最大池龄拒绝。
- [x] 3.2 在激活事务中不可变记录机会类型、GMGN激活价格和规则版本，将80% high-water改为只相对该基线计算。
- [x] 3.3 实现有限旧终态重开及审计，只处理从未发送且原因为`POOL_TOO_OLD`、`POOL_AGE_OUT_OF_RANGE`或旧首次发现基线`CHASE_LIMIT_EXCEEDED`的候选。
- [x] 3.4 添加30分钟边界、老池复苏、Bonding涨幅不计入、激活后超过80%、有限迁移和已发送不重发测试。

## 4. 正式资格质量

- [x] 4.1 将正式资格最终门槛改为市值`$20K–$300K`且新鲜1m Top20，对排名和成交暂时失败返回WAIT。
- [x] 4.2 保留并回归验证全部GMGN安全硬门槛、数据新鲜度及GMGN主池/CoinGecko固定池权责，不新增API或字段兜底。
- [x] 4.3 一次计算并持久化`totalUsd`与`buyUsdRatio`，要求总额≥`$500`、买入金额占比≥60%，同时保留买入笔数≥60%和最大单笔≤40%。
- [x] 4.4 移除`netBuyUsd>0`资格门槛但保留证据，并添加成交金额、笔数/金额分歧、最大单笔、时间边界及重复成交测试。

## 5. 流动性等待与发送前复核

- [x] 5.1 将非零流动性不足、10秒下降超10%和深度暂时不足改为WAIT并重建首样本，确保120秒窗口不重置；池消失、归零、组成变化和安全硬失败保持终态。
- [x] 5.2 首次完整资格通过时持久化不可刷新参考价；为发送前同池detail/trades增加共享5秒deadline，并检查流动性≥`$10K`、深度≤3%、对手侧下降≤20%、最新成交≤5秒和`-5%～+8%`。
- [x] 5.3 将发送前暂时失败保留为每3秒可重试状态，Telegram调用前再次校验120秒截止、规则版本和原资格参考价。
- [x] 5.4 添加重启恢复、整体超时、参考价不可刷新、上下价格边界、流动性恢复、资格到期和不重复发送测试。

## 6. 模拟入场与快速发布

- [x] 6.1 将模拟入场改为Telegram回执后10秒内同一固定池第一笔有效成交，严格区分`ENTRY_UNAVAILABLE`与窗口未覆盖的`PROVIDER_MISSING`。
- [x] 6.2 直接按当前链/epoch查询具有有效入场和成熟15分钟状态的样本，将`VALIDATING→BETA`门槛改为5条，确保不可评估样本不计数也不阻塞且SOL/BSC独立。
- [x] 6.3 按链与规则版本触发5条/每新增20条报告，并在报告内按机会类型和频道阶段展示入场覆盖率、5m/15m MFE/MAE及两组路径指标与免责声明。
- [x] 6.4 添加入场覆盖、不可评估跳过、分段报告、五条晋级、SUSPENDED epoch和旧样本不回写测试。

## 7. 集成验收与部署准备

- [x] 7.1 更新`.env.example`、运行手册和规则说明，删除旧`$20K–$500K`、6小时池龄、20条连续序号等过期描述，公开卡片不显示内部规则版本。
- [x] 7.2 运行格式、类型、单元/集成测试、OpenSpec严格校验和SOL/BSC保存响应dry-run，确认没有新增API、依赖、评分模型或重复资格管线。
- [x] 7.3 准备数据库备份、有限终态迁移、规则版本切换和回滚清单；部署遵循本地修改测试、推送、服务器拉取与只读日志检查，禁止服务器直接改文件。
