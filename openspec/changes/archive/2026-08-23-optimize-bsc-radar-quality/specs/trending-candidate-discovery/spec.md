## MODIFIED Requirements

### Requirement: 公开雷达与内部激活分离
系统 SHALL 保留 GMGN Top100 内部候选覆盖并按链执行公开 Bonding 雷达排名区间。SOL 继续只公开市值 `$10,000–$100,000`、尚无真实池且当前1m排名Top5的候选；BSC只公开同一市值范围、尚无真实池且当前1m排名第6–10名的候选。两链候选均须连续两次在新鲜1m与5m榜同时存在，或连续三次1m名次严格上升并最终进入各自公开排名区间。一次双榜、BSC Top1–5或公开区间之外的上升只触发内部处理，不得公开卡片；这些公开条件不得成为内部真实池激活或正式资格前置条件。

为保持现有BSC池开放快捷激活机会不变，系统 SHALL 在无真实池候选按旧条件满足持续热度、当前1m Top1–5及市值`$10,000–$100,000`时，以独立`bonding_shortcut_readiness`资格事件记录一次不可变内部快捷事实但不发送公开卡；BSC之后开放真实池时 SHALL 使用该事实而非公开`RADAR`状态判断原快捷路径。部署前BSC `RADAR`已有的`_BONDING_CURVE` activation事件 SHALL 被确定性桥接为同一事实，不得按当前排名猜测。BSC公开第6–10名本身不得授予该事实。shortcut事实不得成为首次真实activation reason；SOL SHALL 继续使用现有公开`RADAR`状态快捷行为。

#### Scenario: 连续双榜进入公开雷达
- **WHEN** BSC Bonding候选连续两个新1m快照均与新鲜5m快照形成双榜且第二次1m排名为6–10、市值为`$10,000–$100,000`
- **THEN** 系统发送一张非正式Bonding雷达卡

#### Scenario: BSC Top1–5仅内部处理
- **WHEN** BSC候选满足持续热度但当前1m排名为1–5
- **THEN** 系统继续内部解析和后续真实池检测，但不得发送公开Bonding雷达

#### Scenario: BSC Top3保留原池开放快捷机会
- **WHEN** 无真实池BSC候选满足原持续热度、市值范围且当前1m排名为3，之后在当前双榜不成立时开放真实池
- **THEN** 系统不发送Bonding雷达但记录内部快捷事实，并允许真实池沿用原池开放快捷路径

#### Scenario: 部署前旧Top3雷达保留快捷机会
- **WHEN** 部署前BSC Top3候选已有`_BONDING_CURVE` activation事件和`RADAR`状态，部署后首个处理快照直接显示池开放且当前双榜不成立
- **THEN** 系统以迁移桥接的内部事实沿用原池开放快捷路径并进入`PREHEAT`

#### Scenario: BSC Top7公开不新增池开放快捷机会
- **WHEN** 无真实池BSC候选以当前1m排名7发送Bonding雷达但从未满足内部快捷事实，之后在当前双榜不成立时开放真实池
- **THEN** 系统不得仅因已有公开雷达进入原池开放快捷路径

#### Scenario: SOL公开规则保持不变
- **WHEN** SOL Bonding候选满足原Top5公开门槛
- **THEN** 系统继续按原规则发送非正式Bonding雷达并保留原池开放快捷行为，不应用BSC第6–10名区间

#### Scenario: 后排名次上升仅内部处理
- **WHEN** 候选1m排名从90升至70再升至50
- **THEN** 系统保留并解析内部候选但不得公开雷达卡

### Requirement: 真实池雷达使用独立公开门槛
系统 SHALL 仅在GMGN最大池地址有效、GMGN流动性至少`$10,000`且满足链级公开投影时展示“真实池验证中”。SOL继续使用市值`$20,000–$300,000`和当前1m排名Top20的现有公开门槛。BSC只公开同一市值范围、当前1m排名第6–10名且机会类型为新池的候选；BSC复苏机会不得创建或升级公开雷达。BSC新池仍按现有内部激活条件进入`PREHEAT`，公开条件不得延迟、拒绝或重置其内部资格。系统 SHALL 以独立`radar_public_readiness`资格事件及当前规则版本保存BSC已满足公开首发的事实；新真实池事实 SHALL 由公开投影在首次发送前根据当前新鲜排名、市值、池证据和流动性按需幂等写入，不得限定在激活瞬间。已有SENT雷达时只编辑原卡；旧规则遗留的BSC PENDING雷达只有同时存在当前规则版本的公开事实，并且当前重新满足BSC Bonding或新真实池首发门槛时才可首次发送，SOL PENDING恢复保持现有行为。未达到公开门槛不得阻止内部等待、固定池验证或正式资格。

#### Scenario: Bonding雷达升级
- **WHEN** 已有SENT Bonding雷达的候选开放真实池并满足其链级真实池公开门槛
- **THEN** 系统编辑原雷达卡为“真实池验证中”且不得创建第二张

#### Scenario: 直接发现真实池
- **WHEN** 未发送Bonding雷达的候选首次公开时已存在真实池并满足其链级真实池公开门槛
- **THEN** 系统创建一张“真实池验证中”雷达卡

#### Scenario: 真实池公开热度不足
- **WHEN** 真实池候选不满足其链级公开排名或机会类型门槛
- **THEN** 系统不得公开真实池雷达，但保留内部候选和资格流程

#### Scenario: BSC Top1–5新池继续内部资格
- **WHEN** BSC新池按现有内部条件激活且当前1m排名为1–5
- **THEN** 系统不得公开真实池雷达，但仍进入`PREHEAT`并可继续完整正式资格

#### Scenario: BSC第11–20名新池继续内部资格
- **WHEN** BSC新池按现有内部条件激活且当前1m排名为11–20
- **THEN** 系统不得公开真实池雷达，但仍进入`PREHEAT`并可继续完整正式资格

#### Scenario: BSC Top3新池稍后进入公开区间
- **WHEN** BSC新池在Top3进入`PREHEAT`且未公开，之后以fresh 1m排名7并满足当前市值、池证据和流动性首发门槛
- **THEN** 公开投影幂等写入当前规则版本的公开事实并可首次发送“真实池验证中”

#### Scenario: BSC复苏只进入内部资格
- **WHEN** BSC老池复苏满足现有持续热度及完整内部激活条件
- **THEN** 系统不得创建或升级公开雷达，但仍允许其通过固定池资格后进入验证或正式频道

#### Scenario: 旧PENDING雷达不补发
- **WHEN** 旧规则留下BSC PENDING雷达，而候选在新规则下属于BSC Top1–5、11–20新池、复苏或已进入终态
- **THEN** 系统不得因PENDING记录存在而发送“热度等待”、真实池、拒绝或过期卡片；只有候选之后重新满足当前首发门槛才可发送

#### Scenario: 旧Bonding PENDING需要当前公开事实
- **WHEN** 旧BSC RADAR/PENDING候选当前排名为7但在当前规则版本下尚未重新形成连续双榜或三次上升
- **THEN** 系统不得发送；候选之后满足当前持续热度并写入当前公开事实且仍满足首发范围时才可发送

#### Scenario: 旧真实池PENDING没有当前公开事实
- **WHEN** BSC候选由旧规则进入`PREHEAT`并留下PENDING，但没有当前规则版本的公开首发事实
- **THEN** 系统不得仅凭`PREHEAT`状态发送；候选之后以fresh 1m重新满足新池、排名6–10、市值、池证据及流动性门槛时，公开投影可写入当前事实并首次发送

#### Scenario: SOL PENDING恢复保持不变
- **WHEN** SOL旧PENDING候选满足现有SOL恢复与公开条件
- **THEN** 系统继续按现有SOL行为发送或编辑雷达，不应用BSC PENDING限制

#### Scenario: SOL真实池公开规则保持不变
- **WHEN** SOL真实池候选满足原市值、Top20、池地址和流动性公开门槛
- **THEN** 系统继续按原规则创建或升级公开雷达
